import type { BIResponse } from "@/types";
import { demoRows } from "@/lib/demo";
export type BIContext={demoMode:boolean};export interface BIProvider{ask(question:string,context:BIContext):Promise<BIResponse>}
export class RuleBasedBIProvider implements BIProvider{async ask(question:string,context:BIContext):Promise<BIResponse>{const q=question.toLowerCase();const retention=q.includes("retention");const sql=retention?"SELECT DATE_TRUNC('month', order_date) AS month, COUNT(DISTINCT customer_id) AS customers\nFROM orders\nGROUP BY 1\nORDER BY 1":"SELECT DATE_TRUNC('month', order_date) AS month, SUM(revenue) AS revenue\nFROM orders\nGROUP BY 1\nORDER BY 1";return {answer:context.demoMode?"Demo analysis shows revenue trending upward, with the strongest performance in June. Configure Snowflake to answer using your live warehouse.":"Your live Snowflake query completed successfully.",sql,chart:{type:"area",title:retention?"Monthly customer activity":"Monthly revenue",xKey:"month",yKeys:[retention?"orders":"revenue"],data:demoRows},confidence:context.demoMode?.72:.9,semanticModel:"ORDERS",warehouse:process.env.SNOWFLAKE_WAREHOUSE||"Demo warehouse",executionTime:124,rows:demoRows.length,recommendations:["Compare performance by customer segment","Pin this trend to a dashboard"],insights:["Revenue has risen 69% from January to June."],sources:["Semantic model: ORDERS"]}}}
