import "server-only";
import snowflake from "snowflake-sdk";
export const snowflakeConfigured=()=>Boolean(process.env.SNOWFLAKE_ACCOUNT&&process.env.SNOWFLAKE_USERNAME&&process.env.SNOWFLAKE_PASSWORD);
export async function connection(){if(!snowflakeConfigured())throw new Error("Snowflake credentials are not configured. Add them to .env.local.");const conn=snowflake.createConnection({account:process.env.SNOWFLAKE_ACCOUNT!,username:process.env.SNOWFLAKE_USERNAME!,password:process.env.SNOWFLAKE_PASSWORD!,role:process.env.SNOWFLAKE_ROLE,warehouse:process.env.SNOWFLAKE_WAREHOUSE,database:process.env.SNOWFLAKE_DATABASE,schema:process.env.SNOWFLAKE_SCHEMA,authenticator:process.env.SNOWFLAKE_AUTHENTICATOR});await new Promise<void>((resolve,reject)=>{const timer=setTimeout(()=>{conn.destroy(()=>undefined);reject(new Error("Snowflake connection timed out after 15 seconds. Verify SNOWFLAKE_ACCOUNT, network/firewall access, and your authentication method."))},15000);conn.connect(e=>{clearTimeout(timer);e?reject(e):resolve()})});return conn}
