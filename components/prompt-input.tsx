"use client";import { useRouter } from "next/navigation";import { useState } from "react";
export function PromptInput({onAsk}:{onAsk?:(q:string)=>void}){const [q,setQ]=useState("");const router=useRouter();const submit=()=>{if(!q.trim())return;if(onAsk)onAsk(q);else router.push(`/chat?q=${encodeURIComponent(q)}`)};return <><div className="ask"><textarea value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();submit()}}} placeholder="Ask anything about your business..." aria-label="Business question" rows={2}/><button className="send" onClick={submit}>Ask</button></div><div className="chips">{["What were sales last quarter?","Top 10 products by revenue","Monthly customer retention","Revenue by state"].map(x=><button key={x} onClick={()=>setQ(x)}>{x}</button>)}</div></>}
