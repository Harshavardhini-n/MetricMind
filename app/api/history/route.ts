import { NextResponse } from "next/server";export async function GET(){return NextResponse.json({success:true,data:[{id:"q_1",question:"Monthly revenue performance",when:"12 min ago",sql:"SELECT month, SUM(revenue) FROM orders GROUP BY 1"},{id:"q_2",question:"Top products by revenue",when:"Yesterday",sql:"SELECT product, SUM(revenue) FROM orders GROUP BY 1"}],meta:{demoMode:true}})}
