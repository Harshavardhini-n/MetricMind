const agents=[["SQL Agent","Produces validated read-only SQL"],["Reasoning Agent","Plans metrics and semantic context"],["Visualization Agent","Selects clear chart types"],["Insight Generator","Finds actionable changes"],["Validation Agent","Checks trust and query safety"],["Snowflake Agent","Executes warehouse operations"],["Semantic Layer Agent","Resolves metrics and lineage"]];
export default function Agents(){return <><div className="eyebrow">Specialized orchestration</div><h1 className="page-title">AI Agents</h1><div className="grid">{agents.map(([name,purpose],i)=><article className="card" key={name}><span className="status"><i className="dot"/> Healthy</span><h3 style={{marginTop:15}}>{name}</h3><p className="muted">{purpose}</p><div className="bar"><span style={{width:`${72+i*3}%`}}/></div><p className="muted">Latency {85+i*18} ms</p></article>)}</div></>}
