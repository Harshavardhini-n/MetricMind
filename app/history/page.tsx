"use client";import { useEffect,useState } from "react";import { api } from "@/lib/api";type Item={id:string;question:string;when:string;sql:string};
export default function History(){const [items,setItems]=useState<Item[]>([]);useEffect(()=>{api<Item[]>("/api/history").then(r=>setItems(r.data||[]))},[]);return <><div className="eyebrow">Knowledge trail</div><h1 className="page-title">Query History</h1><div className="card"><table className="table"><thead><tr><th>Question</th><th>When</th><th>Generated SQL</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.question}</td><td className="muted">{x.when}</td><td><code>{x.sql}</code></td></tr>)}</tbody></table></div></>}
