export default function Settings(){return <><div className="eyebrow">Workspace preferences</div><h1 className="page-title">Settings</h1><div className="two"><div className="card"><h3>Snowflake connection</h3><p className="muted">Credentials are read only on the server from <code>.env.local</code>. They are never exposed to the browser.</p><button className="button">Check connection</button></div><div className="card"><h3>LLM provider</h3><p className="muted">Rule-based provider is active. Add an OpenAI provider adapter when you are ready.</p><span className="badge">No API key exposed</span></div><div className="card"><h3>Theme</h3><p className="muted">Dark enterprise theme is active.</p></div><div className="card"><h3>Production checklist</h3><p className="muted">Add authentication, tenant isolation, rate limiting, audit logs, and durable storage before production launch.</p></div></div></>}
